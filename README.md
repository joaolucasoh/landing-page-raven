# Raven Tech — Landing (prototype)

This repo contains a minimal React + Vite + Tailwind scaffold for a premium, dark-themed landing page for Raven Tech.

Quick start:

1. Install dependencies:
```
npm install
```
2. Run development server:
```
npm run dev
```

Notes:
- Built with React, TailwindCSS and Framer Motion (animations).
- Sections are implemented as separate React components under `src/components`.
- Tailwind configuration includes the brand yellow as `raven-yellow`.
